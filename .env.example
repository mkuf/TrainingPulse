# Strava API Credentials
# Get these from https://www.strava.com/settings/api
STRAVA_CLIENT_ID=your_client_id_here
STRAVA_CLIENT_SECRET=your_client_secret_here

# PostgreSQL
POSTGRES_USER=strava
POSTGRES_PASSWORD=changeme_to_a_secure_password
POSTGRES_DB=strava_fitness

# App settings
# The URL where this service is reachable (used for OAuth callback)
APP_BASE_URL=http://localhost:8000

# Heart Rate and Power Settings
# If set, these values will override data from Strava. 
# If not set, the app will try to fetch them from Strava, falling back to hard defaults.
# MAX_HR=190
# REST_HR=60
# FTP=250

# Sync interval in minutes
SYNC_INTERVAL_MINUTES=15
